const About = {
    height: "100vh",
    position: "relative",
}

const container = {
    display: "flex",
    alignItems: "center",
    position: "absolute",
    top: "50%",
    transform: "translateY(-50%)"
}

const title = {
    margin: "0 auto",
    minWidth: "475px",
    width: "30%",
    fontSize: "70px",
    lineHeight: "60px",
    fontWeight: "bold",
}

const name = {
    margin: "0"
}

const subtitle = {
    width: "50%",
    margin: "0 auto",
}

export const styles = {
    About,
    container,
    title,
    subtitle,
    name,
}