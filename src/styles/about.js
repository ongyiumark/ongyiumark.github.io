const About = {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
}

const aboutContainer = {
    display: "flex",
    alignItems: "center",
    position: "absolute",
}

const profile = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
}

const profileImg = {
    maxWidth: "200px",
    borderRadius: "100%",
    paddingBlock: "20px"
}

const title = {
    margin: "0 auto",
    minWidth: "475px",
    width: "30%",
    fontSize: "60px",
    lineHeight: "60px",
    fontWeight: "700",
}

const profileName = {
    margin: "0"
}

const subtitle = {
    width: "50%",
    margin: "0 auto",
    paddingInline: "20px"
}

export const styles = {
    About,
    aboutContainer,
    profile,
    title,
    subtitle,
    profileName,
    profileImg,
}