const LastPage = {
    height: "100vh",
    position: "relative",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
}

const title = {
    fontSize: "70px",
    lineHeight: "60px",
    fontWeight: "bold",
    padding: "0",
    margin: "0"
}

const subtitle = {
    width: "60%"
}

export const styles = {
    LastPage,
    title,
    subtitle,
}