const LastPage = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
}

const title = {
    fontSize: "70px",
    lineHeight: "60px",
    fontWeight: "700",
    padding: "0",
    margin: "0"
}

const subtitle = {
    width: "60%"
}

export const styles = {
    LastPage,
    title,
    subtitle,
}