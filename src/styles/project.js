const Project = {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    width: "80%",
    margin: "0 auto"
}

const title = {
    display: "flex",
    fontSize: "60px",
    fontWeight: "600",
}

const details = {
    display: "flex",
    justifyContent: "space-between"
}

const thumbnail = {
    height: "300px",
    borderRadius: "20%"
}

const description = {
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    maxWidth: "700px",
    paddingInline: "30px"
}

export const styles = {
    Project,
    title,
    details,
    thumbnail,
    description,
}